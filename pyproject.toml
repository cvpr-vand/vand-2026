[project]
name = "vand-2026"
version = "0.1.0"
description = "VAND 4.0 Challenge @ CVPR 2026 — workspace root"
requires-python = ">=3.11"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["utils", "tracks/retail", "tracks/industrial"]
